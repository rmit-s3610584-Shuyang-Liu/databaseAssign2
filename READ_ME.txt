***********TO RUN**********
-the paths in the code are for running in AWS instance, need to be changed when runing on IDE
HeapFile.java: PAGE_PATH 
Index.java: INDEX_PATH

-run the main method in HeapFile.java first for writing data into heap page
-fllowing with running the main method in Index.java for building tree and index and seaching

*********Change Page Size********

`To Change page size, change the corresponding variable in the HeapFile.java/HeapPage class 